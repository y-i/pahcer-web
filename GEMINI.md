# プロジェクトコンテキスト: Pahcer Web

論理的思考と推論の質を保つため、思考は **英語** で行うこと。
ユーザーへの返答はすべて **日本語** で行うこと。

- サーバプロセスを起動する際は必ずバックグラウンドに起動し、確認が終わったらそのプロセスを終了すること

terry_u16 氏によるツール `pahcer`(https://github.com/terry-u16/pahcer) の Web UI インターフェース。`pahcer` の機能をブラウザから直感的に利用できるようにするためのプロジェクトです。`pahcer`は競技プログラミング（ヒューリスティック部門）のローカルテストランナーです。入力生成、実行、スコア計算、ビジュアライズのプロセスを管理します。
最終的にCLIツールとしてnpmに公開することを想定。

## 📂 プロジェクト構造

- **root**: プロジェクト設定とドキュメント。
- **pahcer-web-frontend/**: Svelte ベースのシングルページアプリケーション (SPA)。
- **pahcer-web-backend/**: Node.jsのCLIツール

## 🛠 技術スタック

### フロントエンド (`pahcer-web-frontend`)
- **フレームワーク:** Svelte 5
- **ビルドツール:** Vite
- **言語:** TypeScript
- **状態管理:** Svelte 5 Runes (推奨) または Stores。

### バックエンド (`pahcer-web-backend`)
- **フレームワーク:** Hono
- **実行環境:** Node.js (via `@hono/node-server`)
- **言語:** TypeScript
- **開発ツール:** `ts-node` (開発用実行)

## 📝 コーディング規約

- **言語:** TypeScript 必須。`any` 型の使用を避け、インターフェースや型定義を適切に行うこと。
- **モジュール:** プロジェクトは ESM (`type: "module"`) を採用。`import`/`export` 構文を使用すること。
- **Svelte:** Svelte 5 の機能（`$state`, `$derived` などの Runes）を優先的に使用すること。従来のStore や export let 構文は原則使用禁止とする。
- **バックエンド:** Hono のハンドラーを肥大化させない。ロジックが複雑になる場合は、ルート定義から分離すること。

## 🤖 AI への指示

1.  **言語設定:**
    - **思考プロセス:** 論理的思考と推論の質を保つため、思考は **英語** で行う。
    - **応答:** ユーザーへの返答はすべて **日本語** で行う。

2.  **コード変更:**
    - ライブラリを用いる際はドキュメントを確認すること。
    - Webアプリケーションはフロントエンドのデザインのベストプラクティスに従うこと。
    - 新しいライブラリを提案する前に、必ず `package.json` を確認する。
    - 既存のプロジェクト構造（フロントエンドとバックエンドの分離）を尊重する。
    - コード修正時は、厳格な型安全性を確保する。

3.  **コンテキスト理解:**
    - このツールは別の CLI ツール (`pahcer`) のラッパーであることを意識する。`pahcer` 本体の仕様について不明な点があれば、適宜ユーザーに確認すること。

4. **許可について**
    - ユーザーに確認した際にユーザーが`ok`とのみ、返信した場合は確認事項を承認したとして扱うこと。
    - ユーザーが`go`とのみ入力した場合、次のタスクを開始すること。
      - 終了したタスクにはチェックボックスのチェックを付けること
